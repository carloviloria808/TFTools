# Screenshots

Drop PNG screenshots here with these exact filenames so they render in the main README:

| Filename | What to capture |
|----------|-----------------|
| `tier-list.png` | The Comp Tier List page (`/compositions`) |
| `comp-detail.png` | A composition's detail view (board + sidebar) |
| `team-builder.png` | The Team Builder (`/builder`) with a few units placed |
| `shop-odds.png` | The Shop Odds page (`/odds`) |

**Tips**
- Capture at a normal desktop width (~1280px) for consistent sizing.
- PNG keeps text crisp; keep each file under ~1 MB if you can.
- After adding them: `git add docs/screenshots && git commit -m "Add screenshots" && git push`
